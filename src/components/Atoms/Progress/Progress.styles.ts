import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: "row",
    height: 4,
    marginHorizontal: 16,
  },
  progressBar: {
    height: 4,
    borderRadius: 16,
  },
});
